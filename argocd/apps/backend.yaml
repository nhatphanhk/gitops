apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: backend
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/nhatphanhk/gitops.git
    path: k8s/backend
  destination:
    server: https://kubernetes.default.svc
    namespace: demo
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - ServerSideApply=true
  # Bỏ qua selector drift của backend-canary service
  # (Argo Rollouts tự quản lý selector này)
  ignoreDifferences:
    - group: ""
      kind: Service
      name: backend-canary
      namespace: demo
      jsonPointers:
        - /spec/selector
